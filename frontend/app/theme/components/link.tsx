import { ComponentSingleStyleConfig } from '@chakra-ui/react';

export const LinkStyle: ComponentSingleStyleConfig = {
  baseStyle: {
    fontWeight: 500,
    textDecoration: 'underline',
  },
  sizes: {},
  variants: {},
  defaultProps: {},
};
